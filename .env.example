# URL del endpoint de sismos (incluye path /sismos).
# El componente MapaSismos.jsx hace fetch(`${PUBLIC_API_URL}?anio=...`),
# por eso la URL aqui debe incluir el path completo del recurso.
PUBLIC_API_URL=https://api.marc.com.pe/sismos
